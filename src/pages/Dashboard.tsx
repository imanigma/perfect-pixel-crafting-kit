
import React from "react";
import { DashboardLayout } from "@/components/dashboard";

const Dashboard = () => {
  return (
    <DashboardLayout>
      {/* The DashboardLayout already contains the main dashboard content */}
    </DashboardLayout>
  );
};

export default Dashboard;
